#include <stdio.h> 
int main() 
{ 
    
     double x, y;
    printf("Enter a: ");
    scanf("%lf", &a);
    printf("Enter b: ");
    scanf("%lf", &b);
    
    x = x + y; // x now becomes  
    y = x - y; // y becomes  
    x = x - y; // x becomes 
   printf("Results after changing values: x = %d, y = %d", x, y); 
   return 0; 
}
